# Configuration de Ansible pour le déploiement
[defaults]
inventory = inventory.ini
host_key_checking = False
deprecation_warnings = False
log_path = ansible.log

[privilege_escalation]
become = True
become_method = sudo
become_user = root
become_ask_pass = True
